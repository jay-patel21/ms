apiVersion: v1
kind: Service
metadata:
  name: service-a-internal
  namespace: node-kube-prod
  labels:
    app: service-a
spec:
  clusterIP: None
  selector:
    app: service-a
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP

