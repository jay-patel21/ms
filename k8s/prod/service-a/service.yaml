apiVersion: v1
kind: Service
metadata:
  name: service-a
  namespace: node-kube-prod
  labels:
    app: service-a
spec:
  type: LoadBalancer
  selector:
    app: service-a
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP

